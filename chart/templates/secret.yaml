{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "venturebell.fullname" . }}
  labels:
    {{- include "venturebell.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if not .Values.secret.bellToken }}
  {{- fail "set either existingSecret or secret.bellToken — the server refuses to start without BELL_TOKEN" }}
  {{- end }}
  BELL_TOKEN: {{ .Values.secret.bellToken | quote }}
  {{- if .Values.secret.pushoverToken }}
  PUSHOVER_TOKEN: {{ .Values.secret.pushoverToken | quote }}
  PUSHOVER_USER: {{ required "secret.pushoverUser is required alongside secret.pushoverToken" .Values.secret.pushoverUser | quote }}
  {{- end }}
{{- end }}
